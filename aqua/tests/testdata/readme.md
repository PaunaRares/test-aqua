# TestData Folder

This folder is intented for storing test-data assets, like:
* database data files (*.sql)
* configuration files (config.xml, articles.json, etc)

Do not put in here any binary or large files, so that they do not end up in git, like:

- *.zip, *.mp4, *.png, etc